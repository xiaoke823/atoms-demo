# Atomix · 笔试文档

> **一句话**:描述一个想法,AI 员工团队(产品经理 → 架构师 → 工程师 → 质检)流水线生成一个可运行的网页应用,全过程以"对话剧场"形态实时呈现。
>
> 阅读本文约需 10 分钟。代码在本仓库,部署手册见 `DEPLOY.md`。

---

## 1. 功能总览

| 功能 | 入口 | 说明 |
|---|---|---|
| 一句话生成应用 | 首页输入框 | 四角色流水线,SSE 实时流式呈现,产出单文件 HTML 应用 |
| 实时预览 | 生成页右栏 | 流式代码视图 → iframe 实时渲染(沙箱隔离) |
| 聊天迭代 | 工作台输入框 | 自然语言提修改需求,完整重生成 HTML,规则复检 |
| 作品发布 | 工作台 | 生成公开链接 `/p/:slug`,奖 5 积分 |
| 广场 + Remix | `/explore` | 浏览他人作品,一键复制谱系再创作,原作者得 2 积分,邮箱脱敏展示 |
| 积分系统 | 顶栏 | 注册 100;生成 -10;迭代 -2;签到 +20;发布 +5;**失败自动全额退款**;流水全程可审计 |
| 账号 | 注册/登录 | JWT + scrypt 密码哈希 |

## 2. 技术栈与整体架构

**栈**:Next.js 16(App Router)+ React 19 + TypeScript 5 / Tailwind CSS v4 / SQLite(better-sqlite3,WAL)/ jose JWT / 智谱 glm-4.5-flash(OpenAI 兼容协议)/ Vitest(37 用例)

```
首页输入想法 ──▶ /workspace/new ──POST /api/projects (SSE 长连接)──▶ 服务端生成管线
                                                                      │
        ┌─────────────────────── pipeline.ts ────────────────────────┐│
        │ ① PM Emma     chatStream ▶ 需求 JSON(名称/功能/简介)         ││ SSE 事件流
        │ ② 架构师 Bob   chatStream ▶ 方案 JSON(布局/模块/配色)         ││ stage_start/delta/
        │ ③ 工程师 Alex  chatStream ▶ 完整单文件 HTML(流式)            ││ preview/stage_done/
        │ ④ 质检 Iris    规则检查(零成本) + LLM 语义审查               ││ done/error
        │ ⑤ 不通过 ▶ 打回工程师修复一轮                                 ││
        └────────────────────────────┬───────────────────────────────┘│
                                     │ 全部完成                        ▼
                              落库 SQLite(4 表)              前端"剧场"状态机折叠渲染
                                                          (实时生成与历史回放同一套代码)
```

数据模型 4 张表:`users`(含积分)/ `projects`(含 remix 谱系、发布 slug)/ `messages`(每角色产物,回放即重放)/ `credit_transactions`(积分流水)。

---

## 3. 实现思路与关键取舍

> 每一项按"**备选 → 选择 → 理由 → 代价**"陈述。取舍的诚实比取舍本身更重要。

### 3.1 流水线分解:四角色而非单次大 prompt

- **备选**:一个大 prompt 直接"想法 → 完整应用"。
- **选择**:PM(收敛需求)→ 架构师(定结构与配色)→ 工程师(写码)→ QA(规则 + 语义)→ 修复,共 4~6 次 LLM 调用。
- **理由**:①每步输出可校验(JSON 解析失败自动重试一次,再降级为兜底文案);②QA 的规则检查零成本且确定性,先于 LLM 审查执行;③中间产物入库,历史可回放。
- **代价**:总时长 3~11 分钟(免费档模型),token 成本数倍。demo 场景可接受;生产可换更快模型压缩。

### 3.2 前端核心:纯函数状态机,实时 = 回放

- **备选**:为"实时生成"和"历史查看"写两套渲染。
- **选择**:`theater.ts` 把 SSE 事件流归约为单一状态的纯函数(`applyEvent` / `cardsFromHistory`),实时生成是"喂事件",历史回放是"重放消息表",**同一套代码**。
- **理由**:消除两套实现必然产生的展示不一致(本项目真实踩过:生成中显示原始 JSON、回放时没有——见 §4 案例三);纯函数可直接单测(6 个用例覆盖时间线顺序/事件过滤)。
- **代价**:状态结构需一次设计到位(用户消息也建模为卡片才能保持时间线一致)。

### 3.3 SSE 协议:delta 带 `status` 标记

- **问题**:PM/架构师的 LLM 原始输出是 JSON,工程师是 HTML——直接流给用户就是"满屏代码垃圾";但不流又显得假死。
- **选择**:协议层区分两类 delta——人工提示行(`status:true`,展示)与模型原始输出(缺省,**只计字数做进度**,永不渲染原文)。
- **理由**:一行字段同时解决"可读性"与"过程感"(正在思考 N 字 / 已生成 N 字符),且服务端零过滤逻辑。

### 3.4 LLM 层可靠性:分层防御(全部来自真实故障)

| 层 | 机制 | 对应的真实故障 |
|---|---|---|
| 重试 | 429/5xx/连接失败退避 1s/2s 重试;**仅在未推送任何输出前可重试**(否则内容重复) | 上游免费档瞬时限流直接击穿整次付费生成 |
| 看门狗·头 | 响应头等待 300s 宽限 | glm-4.5 是推理模型,**思考期间不返回响应头**(实测 69s),旧 120s 限值误杀合法思考 |
| 看门狗·流 | 头到达即武装 120s 空闲检测 | 修复了"头到达后看门狗被解除、死连接永久挂起"的间隙 bug |
| 断流检测 | 客户端发现流关闭但无 done/error 时合成 error 事件 | 服务进程中途死亡时页面永远转圈 |

### 3.5 积分:账务型设计,不是计数器

- **选择**:扣减用**条件 UPDATE**(`... SET credits = credits - ? WHERE credits >= ?`)+ 同事务写流水,原子防超扣;任何失败路径自动退款并记 `refund` 流水。
- **理由**:积分是本应用唯一的"资源约束",等价于虚拟资金——**宁可少扣不可错账**。流水表让每一次变动可追溯(本文 §4 案例一正是靠流水表还原了用户报告的故障时间线)。

### 3.6 存储与限流:刻意的小,写在明处

| 项 | 选择 | 明确的代价与触发条件 |
|---|---|---|
| SQLite + WAL | 单文件零运维,同步 API 无连接池 | 多副本/水平扩展时需迁移 Postgres |
| 内存 Map 限流(生成 30s/迭代 15s) | 零依赖;失败自动释放窗口 | **只支持单实例**;多实例需 Redis |
| 生成绑定 SSE 请求生命周期 | 无队列,实现最短 | 进程重启丢进行中的生成(退款兜底;进程被强杀时退款也来不及) |

### 3.7 安全

- JWT(HS256)+ scrypt + `timingSafeEqual`;生成产物在 **sandbox iframe**(`allow-scripts`,无同源)中渲染,恶意代码碰不到父页面与 Cookie。
- 已知弱点(诚实声明):token 存 localStorage(XSS 窃取面),生产应改 HttpOnly Cookie——见 §6 P0。

### 3.8 部署:standalone + Docker,明确排除 Serverless

生成需要 3~11 分钟的 SSE 长连接 + SQLite 文件写入,**与 Vercel 类平台的函数时限/只读文件系统结构性冲突**,故选择单容器长驻进程:三阶段 Docker 构建、原生模块显式兜底拷贝(file tracing 实测不追踪 better-sqlite3)、非 root 运行、数据卷 + 健康检查。Nginx 反代必须配 `proxy_buffering off` + 长读超时(SSE 关键)。

---

## 4. 三个真实调试案例(方法论展示)

> 三者都遵循同一方法:**先取证定位根因,再动手修**;不靠猜测。

**案例一:用户报"闪一下 → 太频繁 → 速率受限制"。**
不猜。查积分流水表还原时间线:03:48:27 有一笔 `-10` 既无退款也无项目产出 → 证明请求真实发出且生成中途死亡;03:49:39 一笔同秒退款 → 证明是上游 LLM 秒拒(429,报错文案系供应商透传);"太频繁"来自自身 30s 限流。一次用户描述,拆出三个独立缺陷(进程死亡无提示 / 流断无提示 / 失败不释放限流),分别修复。

**案例二:点击"开始创造"后页面弹回首页,但请求已发出。**
流水证明请求发出 → 说明 `/workspace/new` 挂载过 → 必有东西把它弹回。读代码:守卫 effect 消费 sessionStorage 后非幂等;React 严格模式下全局 auth 双执行,第二次 `setUser(新对象)` 触发守卫重跑,取不到已消费的想法 → `replace("/")`。加 `consumed` ref 使守卫幂等,一次修复 100% 复现的问题。

**案例三:PM 阶段满屏 JSON,完成后消失。**
根因是"实时渲染原始流,回放渲染解析产物"两套逻辑不一致——这正是引入纯函数状态机 + `status` 协议(§3.2/§3.3)的直接动因,并用 6 个单测锁定行为。

---

## 5. 当前完成程度

**已完成(有验证)**
- 全部 §1 功能;四角色流水线 + 一轮修复;SSE 实时剧场与历史回放一致性
- 账务型积分(原子扣减/退款/流水);发布/Remix/公开页/邮箱脱敏
- LLM 层四重可靠性(§3.4);断流检测;限流失败释放
- 测试:**37 个 Vitest 用例**(管线事件语义 / 剧场状态机 / 限流 / 重试与双阶段看门狗 / SSE 断流 / JSON·HTML 提取 / DB 账务 / scrypt 往返);另有 `scripts/e2e.mjs` 18 项端到端冒烟(真实 LLM)
- 部署套件:Dockerfile(standalone、原生模块兜底、非 root)+ compose(数据卷/健康检查)+ `/api/health` + DEPLOY.md;standalone 产物本地实跑验证

**做了但简化(已知边界)**
- 迭代 = 全量重生成 HTML(非增量 diff);QA 修复仅一轮
- PM 输出非法 JSON 时降级为兜底文案(`degraded` 标记)
- 测试集中在纯函数层,组件/端到端未自动化(e2e 为手动脚本)

**未做(如实声明)**
- 任务队列:生成不可从进程重启中恢复(见 §6 P0)
- HttpOnly Cookie 化认证;内容合规审核;管理后台;移动端系统化适配与测试
- 可观测性:结构化日志、指标、链路追踪

## 6. 若继续投入:扩展路线与优先级

> 判据:**P0 = 正确性/资金/核心路径阻断;P1 = 体验与成本;P2 = 规模化**(用户量证明之前不预建)。

**P0(先做,约数天)**
1. **生成任务化(任务表 + 状态机)**:生成过程落库为任务记录,SSE 仅作推送;进程重启可恢复或明确退款。当前"强杀进程即丢积分"是唯一的资金级缺陷。
2. **认证改 HttpOnly Cookie**:消除 localStorage 的 XSS 窃取面,代价小收益确定。
3. **工程师输出截断容错**:maxTokens 耗尽导致 HTML 不完整时(`finish_reason=length`),从未闭合围栏提取并标记"截断,建议迭代补全",把硬失败变成可恢复。

**P1(次做,一两周量级)**
1. 迭代改增量编辑(只重写受影响区块),token 成本与时长降一个量级。
2. 多供应商 failover(免费档限流时自动切换),LLM 层协议已就绪,只差路由。
3. 生成进度更细的阶段反馈与预计剩余时间(基于历史耗时统计)。

**P2(有真实用户量后再做)**
1. Postgres 迁移 + Redis 限流(解除单实例约束)→ 才谈多副本。
2. 可观测性套件(结构化日志 + 生成成功率/时长看板)。
3. 内容审核与频控加固(公开页面被滥用风险)。


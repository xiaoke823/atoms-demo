# 复制为 .env.production 并填写。此文件不要提交到 git。

# 必填：JWT 签名密钥。生成命令：openssl rand -hex 32
# 不设置会回退到代码里的开发密钥，等于裸奔！
AUTH_SECRET=

# 必填：LLM 供应商（OpenAI 兼容协议，换供应商只改这三行）
LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
LLM_API_KEY=
LLM_MODEL=glm-4.5-flash
